# Peafowl Python dependencies
# Install with: uv pip install -r requirements.txt
#
# Note: SSS and lwr_cpp are C++ extensions built from source (see CLAUDE.md).
# Note: mpi4py requires libmpi.so on LD_LIBRARY_PATH (see CLAUDE.md).

mpi4py==4.1.1
numpy==2.4.2
torch==2.10.0
torchvision==0.25.0
h5py==3.16.0
cryptography==46.0.5
pybind11==3.0.2
line_profiler==5.0.2
cffi
